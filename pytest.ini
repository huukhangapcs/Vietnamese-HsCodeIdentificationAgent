[pytest]
# Đăng ký custom marks để tránh warning
markers =
    integration: integration tests yêu cầu DEEPSEEK_API_KEY và môi trường đầy đủ

# Mặc định chạy unit tests (không cần API key)
# Chạy integration: pytest -m integration
addopts = -v
testpaths = tests
